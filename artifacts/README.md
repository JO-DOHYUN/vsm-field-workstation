# artifacts

Generated HIL, UI smoke, layout probe, and validation artifacts are written here during local runs.

This directory is intentionally kept out of git except for this README. Copy a specific run summary into `docs/field/` or `history/incidents/` when it becomes source-of-truth evidence.
