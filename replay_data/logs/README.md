# replay_data/logs

VSM runtime session log와 typed capture가 저장되는 프로젝트 내부 운영 폴더다.

예전 AppData 로그는 이 위치로 이관하는 방향을 기준으로 한다. 실제 `.typed`, `.stream`, `.jsonl`, `.log`, `.bin`, `.part` 파일은 git source가 아니다.
