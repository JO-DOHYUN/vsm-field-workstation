# replay_data

프로젝트 내부 runtime data root다.

- `logs/`: session logs, typed captures, migrated AppData logs
- `snapshots/`: replay/export snapshot staging

이 폴더의 실제 capture/log/binary 산출물은 source가 아니며 git에 넣지 않는다. README 파일만 추적해 운영 데이터 위치를 명시한다.
