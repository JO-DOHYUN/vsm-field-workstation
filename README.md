# VSM Field Workstation

VSM is a Windows Qt/C++ evidence-first CAN monitor, logger, replay, decode, graph, and control workstation for the CSM typed evidence/control gateway.

Start here:

- `AGENTS.md`: Codex operating rules
- `START_HERE_KO.md`: Korean onboarding entry
- `BRIEF.md`: current baseline and immediate work
- `INDEX.md`: document map

Generated outputs are intentionally not source: `out/`, `.logs/`, `.ref-replay/`, runtime captures under `replay_data/`, and validation runs under `artifacts/`.
