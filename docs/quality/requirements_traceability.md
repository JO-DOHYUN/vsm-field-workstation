# Requirements Traceability

| Requirement ID | Requirement | Current Owner | Automated Coverage | Manual Acceptance |
| --- | --- | --- | --- | --- |
| RQ-BUILD-001 | Shared preset configure/build path exists | CMake | CI configure/build | fresh-shell configure/build |
| RQ-TEST-001 | `ctest` runs real tests | tests/ | `packet_parser`, `model_pack_validator` | n/a |
| RQ-MODEL-001 | malformed model pack is rejected | `ModelPackValidator` | `model_pack_validator` | load invalid external model |
| RQ-LOG-001 | session logs and typed captures are written under project-local `replay_data/logs` | `StorageRuntime`, `RuntimePaths` | `storage_replay_runtime_paths`, `app_controller_log_flow`, startup smoke | inspect `replay_data/logs` session output |
| RQ-EVIDENCE-001 | COM open alone is not board alive; valid capability and fresh health drive alive/control-capable state | `EvidenceRuntime`, `BoardConnectionState` | `evidence_runtime_foundation`, `app_controller_log_flow` | connect CSM and observe capability/health stale/fault states |
| RQ-CONTROL-001 | host request/write, `CONTROL_ACK`, matching `CAN_TX_RAW`, feedback, and fault remain separate control evidence; operator UI checklist/verdict treats only `CAN_TX_RAW` as actual sent success | `ControlRuntime`, `ControlAuditModel`, `AppController` facade, `ControlPage.qml` | `control_runtime_foundation`, `control_audit_model`, `app_controller_log_flow`, `qml_shell_smoke` | send control command and verify TX success only after `CAN_TX_RAW` |
| RQ-CONTROL-002 | model-pack `control_policy` drives target role, bus role fingerprints, allowed roles, and rpm/steering clamp limits visible in ControlPage and enforced by the gate | `ModelPack`, `BusRoleResolver`, `AppController` facade, `ControlPage.qml` | `model_pack_validator`, `app_controller_log_flow`, `qml_shell_smoke`, `evidence_runtime_foundation` | load field model pack, observe policy row, and verify HIL command limits/target role before arming |
| RQ-TRANSPORT-001 | live production transport has a typed-only runtime boundary and operator diagnostics for parser faults, host TX queue/backpressure, and live delay | `TransportRuntime`, `TransportSession`, `SerialWorker` | `transport_runtime_foundation`, `serial_worker_typed_ingest`, `app_controller_log_flow`, `qml_shell_smoke` | live CSM session remains typed evidence only and Live page TRANSPORT diagnostics are explainable |
| RQ-GRAPH-001 | overview/detail semantics preserved and graph signal toggle keeps list scroll/color state stable through actual QML checkbox click and wrapper probes | `AppController`, QML, `GraphViewportItem` | `analysis_semantics`, `app_controller_replay_flow`, `qml_shell_smoke` | operator graph smoke with real live/replay data |
| RQ-REPLAY-001 | replay seek/speed semantics preserved; typed sessions expose timeline, meta/index/events, type counts, seq gaps, partial/corrupt faults, CAN RX projection, operator verdict, and DLC preservation diagnostics | `AppController`, `ReplayRuntime`, `ReplayEngine`, `TypedReplayReader`, `ReplayPage.qml` | `typed_replay_reader`, `app_controller_replay_flow`, `qml_shell_smoke` | replay operator smoke with field typed capture |
